# Before you publish on GitHub

- [ ] Review `firmware/config.h` — change `WIFI_AP_PASS` if you do not want the default public.
- [ ] Confirm callsign/grid are what you want public.
- [ ] Add schematic under `hardware/` if available.
- [ ] Optional: replace placeholder images in `manual_build/figures/ph_*.png`.
